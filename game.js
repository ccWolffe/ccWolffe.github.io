function setup () {//https://p5js.org/get-started/
  createCanvas(640, 480); }
  
  
}
function draw () {
  if (mouseIspressed) {
    fill(0);
  } else {
    fill(245);
  }
  ellipse(mouse X, mouse Y, 80, 80);
  background();

}
